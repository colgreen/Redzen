<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Version>7.1.0.0</Version>
    <Copyright>Copyright Colin D. Green 2016-2018</Copyright>
    <Description>General purpose C# code library.</Description>
    <PackageLicenseUrl>http://opensource.org/licenses/MIT</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/colgreen/Redzen</PackageProjectUrl>
    <PackageTags>C# .NET rng ziggurat gaussian io sorting timsort xoshiro</PackageTags>
    <PackageReleaseNotes>Next(int) and Next(int,int) now use rejection sampling to ensure unbiased sampling (this is a further improvement over System.Random).
Added IRandomSource.NextDoubleHighRes().
Bounds check fix on Next(int).</PackageReleaseNotes>
    <SignAssembly>true</SignAssembly>
    <AssemblyOriginatorKeyFile>colindgreen.pfx</AssemblyOriginatorKeyFile>
    <Authors>Colin D. Green</Authors>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <Folder Include="Properties\" />
  </ItemGroup>

</Project>